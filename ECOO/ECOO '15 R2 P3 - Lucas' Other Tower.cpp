#include <bits/stdc++.h>
using namespace std;

int N, T;
int TC = 10;

int main() {
  ios_base::sync_with_stdio(false);
  cin.tie(NULL);

  while(TC--) {
    cin >> N >> T;
    cout << 2 * N - 1 << "\n";
  }

  return 0;
}